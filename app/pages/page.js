import Navbar from '../components/Navbar';
import Hero from '../components/Hero';

const Home = () => {
    return (
        <div>
            <Navbar />
            <Hero />
            {/* Sections like benefits, top destinations, etc., can be added here */}
        </div>
    );
};

export default Home;
