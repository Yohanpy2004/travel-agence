import React from 'react';

const Hero = () => (
    <section className="hero">
        <h1>Explore the World with Us</h1>
        <p>Find the best travel agencies, destinations, and plan your dream trip with ease!</p>
    </section>
);

export default Hero;
